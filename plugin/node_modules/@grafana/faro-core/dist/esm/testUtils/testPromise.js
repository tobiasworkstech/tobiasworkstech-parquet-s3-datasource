export function createTestPromise(id) {
    const obj = {
        id,
    };
    obj.promise = new Promise((resolve, reject) => {
        obj.resolve = resolve;
        obj.reject = reject;
    });
    return obj;
}
//# sourceMappingURL=testPromise.js.map