class BinaryOperationVector extends Array {
  constructor(left, right, operation) {
    super();
    const arr = new Array(left.length);
    for (let i = 0; i < arr.length; i++) {
      arr[i] = operation(left[i], right[i]);
    }
    return arr;
  }
}

export { BinaryOperationVector };
//# sourceMappingURL=BinaryOperationVector.js.map
