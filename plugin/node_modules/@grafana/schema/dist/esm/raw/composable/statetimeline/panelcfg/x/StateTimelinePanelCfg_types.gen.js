import { V as VisibilityMode } from '../../../../../common.gen-ebf8a970.js';

const pluginVersion = "10.4.19";
const defaultOptions = {
  alignValue: "left",
  mergeValues: true,
  rowHeight: 0.9,
  showValue: VisibilityMode.Auto
};
const defaultFieldConfig = {
  fillOpacity: 70,
  lineWidth: 0
};

export { defaultFieldConfig, defaultOptions, pluginVersion };
