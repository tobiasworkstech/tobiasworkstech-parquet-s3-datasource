const pluginVersion = "%VERSION%";
const defaultParcaQueryType = "both";
const defaultParcaDataQuery = {
  labelSelector: "{}"
};

export { defaultParcaDataQuery, defaultParcaQueryType, pluginVersion };
